# HUMAN.md — Bright Bots Harness setup

This is a gap report, regenerated by `harness setup`. It describes the distance between what is configured now and what the target tier needs, and it shrinks as prerequisites are satisfied. It contains no secret value — only key names, scopes, and who must act.

## Current state

- Current tier: **0** — Tier 0 — read only, no credential
- Target tier: **2** — Tier 2 — push branches to its own fork and open pull requests
- Overall: **not ready** — 10 of 18 prerequisites satisfied

| # | Prerequisite | Actor | Needed from tier | Status |
|---|---|---|---|---|
| 1 | Nathan's GitHub handle confirmed and written to `.harness/trust.txt` | you | 2 | done |
| 2 | Machine account `jgoetzmann-bot` created | you | 2 | done |
| 3 | `Bright-Bots-Initiative/brightboost` forked into the machine account as `jgoetzmann-bot/brightboost` | you | 2 | done |
| 4 | Actions enabled on the fork | you | 2 | outstanding |
| 5 | Classic PAT on `jgoetzmann-bot`, scope `public_repo` only, present in `.env` as `HARNESS_GITHUB_TOKEN` | you | 2 | done |
| 6 | `claude setup-token` run and saved as repository secret `CLAUDE_CODE_OAUTH_TOKEN` | you | 2 | outstanding |
| 7 | The classic PAT saved as repository secret `HARNESS_GITHUB_TOKEN` | you | 2 | outstanding |
| 8 | `jgoetzmann/bright-bots-harness` made public | you | 2 | done |
| 9 | Branch protection on `main`: one approving review, no force-push | you | 2 | outstanding |
| 10 | `.github/CODEOWNERS` committed naming you for `.harness/`, `prompts/`, `.github/`, `gates.py`, `redact.py` | you | 2 | done |
| 11 | The twelve `harness:*` state labels exist on `jgoetzmann/bright-bots-harness` | you | 2 | done |
| 12 | Pinned tracking issue opened and its number recorded as `TRACKING_ISSUE` | you | 2 | done |
| 13 | Decided whether Nathan reviews proposals; `CODEOWNERS` covers `/proposals/` | you | 2 | done |
| 14 | Verified whether `claude --max-budget-usd` binds under subscription auth | you | 2 | done |
| 15 | Docker Desktop installed and `docker info` succeeds (local mode only) | you | 2 | outstanding |
| 16 | Nathan told this exists, before the first delivery PR arrives | you | 2 | outstanding |
| 17 | `PERMISSION_TIER` raised to 2 | nathaniel | 2 | outstanding |
| 18 | `jgoetzmann-bot` is NOT a collaborator on `Bright-Bots-Initiative/brightboost` — confirmed | nathaniel | 2 | outstanding |

Already satisfied, nothing further to do:

- **Nathan's GitHub handle confirmed and written to `.harness/trust.txt`** — done.
- **Machine account `jgoetzmann-bot` created** — done.
- **`Bright-Bots-Initiative/brightboost` forked into the machine account as `jgoetzmann-bot/brightboost`** — done.
- **Classic PAT on `jgoetzmann-bot`, scope `public_repo` only, present in `.env` as `HARNESS_GITHUB_TOKEN`** — done.
- **`jgoetzmann/bright-bots-harness` made public** — done.
- **`.github/CODEOWNERS` committed naming you for `.harness/`, `prompts/`, `.github/`, `gates.py`, `redact.py`** — done.
- **The twelve `harness:*` state labels exist on `jgoetzmann/bright-bots-harness`** — done.
- **Pinned tracking issue opened and its number recorded as `TRACKING_ISSUE`** — done.
- **Decided whether Nathan reviews proposals; `CODEOWNERS` covers `/proposals/`** — done.
- **Verified whether `claude --max-budget-usd` binds under subscription auth** — done.

## What you need to do

1. **Actions enabled on the fork** (actor: you, id `fork-actions`)
   Why: Disabled by default on forks. Needed only if you want the fork's CI as a second opinion; the harness runs its own gates regardless. Nothing outside the fork's settings page can check this.

2. **`claude setup-token` run and saved as repository secret `CLAUDE_CODE_OAUTH_TOKEN`** (actor: you, id `claude-token`)
   Why: Requires an interactive browser login against your subscription. The harness cannot inspect repository secrets, so confirm this by hand.

3. **The classic PAT saved as repository secret `HARNESS_GITHUB_TOKEN`** (actor: you, id `pat-secret`)
   Why: The scheduled workflows read it from the repository secret; the local `.env` copy never reaches the container (§10.5). Repository secrets are not readable, so confirm by hand.

4. **Branch protection on `main`: one approving review, no force-push** (actor: you, id `branch-protection`)
   Why: B113. The harness's own proposal PRs are subject to it like anything else. Reading protection rules needs administrative access the harness does not have, so confirm by hand.

5. **Docker Desktop installed and `docker info` succeeds (local mode only)** (actor: you, id `docker`)
   Why: Phase 8 only. Local mode runs the same harness in the `bb` container; Actions mode does not need this.
   Confirm with: `docker info`

6. **Nathan told this exists, before the first delivery PR arrives** (actor: you, id `tell-nathan`)
   Why: A bot PR from an unknown account on a public repository, unannounced, is a bad first impression and may be treated as spam.

## Tokens

| `.env` key | Type | Permission set for tier 2 | Where to create it | Who must approve it |
|---|---|---|---|---|
| `HARNESS_GITHUB_TOKEN` | Classic personal access token on `jgoetzmann-bot`, scope `public_repo` **only** | `Metadata`: Read; `Issues`: Read and write; `Pull requests`: Read and write; `Contents`: Read and write; `Workflows`: none; `Administration`: none; `Secrets, Environments, Actions`: none | https://github.com/settings/tokens/new | Nobody: the account owns the fork `jgoetzmann-bot/brightboost` and nothing of the organization's; there is no organization-side approval |

**Why a classic token and not fine-grained (handoff §5.2).** Fine-grained tokens cannot open a pull request from a fork to an upstream repository — a documented, long-standing limitation. A classic token with `public_repo` is the working credential. On an account that owns nothing but the fork, that is a bounded blast radius: it can write to public repositories the account can already write to, which is the fork, and it can open pull requests, which is the point. The one credential is used by `harness/gh.py` and by nothing else (I-11).

What `public_repo` grants, and what it does not, for tier 2:

| Permission | Tier 2 | Note |
|---|---|---|
| `Metadata` | Read | Implied by `public_repo` on public repositories |
| `Issues` | Read and write | Issues and comments in this repository only — never on the product repository (I-14) |
| `Pull requests` | Read and write | Opening the delivery PR from the fork; nothing that closes, accepts or alters a review (I-12) |
| `Contents` | Read and write | Pushing a branch to the fork the account owns; the product repository is not writable because the account is not a collaborator |
| `Workflows` | none | The `workflow` scope is deliberately absent: GitHub rejects any push that touches `.github/workflows/` (I-15) |
| `Administration` | none | No `admin:*` scope, ever |
| `Secrets, Environments, Actions` | none | Never |

The scope that is deliberately absent is `workflow`. GitHub rejects any push that modifies `.github/workflows/`, so "never edit CI to go green" is enforced by the receiving end (I-15), not trusted to the model. `repo`, `admin:*` and every other classic scope are absent too.

## What needs Nathaniel

Everything below needs organization or repository administration. It is gathered here so it can be sent as one request.

1. **`PERMISSION_TIER` raised to 2** (actor: Nathaniel, id `tier`)
   Why: Agree Tier 2 — push branches to its own fork and open pull requests, then set `PERMISSION_TIER=2` in `.env`. Tier 2 also requires `STORE_BACKEND=github` and a non-empty `FORK_REPO`. Nathaniel agrees the tier; you edit the file.
   Confirm with: `harness doctor`

2. **`jgoetzmann-bot` is NOT a collaborator on `Bright-Bots-Initiative/brightboost` — confirmed** (actor: Nathaniel, id `collaborator`)
   Why: The account delivers through pull requests from its own fork and must have no write access upstream (§5.1, review check R5.5). Confirm in the product repository's collaborator settings that it is absent, and keep it so.

## What the harness will never ask for

- Production credentials of any kind — database URLs, deployment keys, cloud accounts.
- Organization administration, or ownership of the organization.
- The `Workflows` permission, or the classic `workflow` scope. Without it GitHub itself rejects any push that modifies `.github/workflows/`, so "never edit CI to go green" stops being a rule the harness is trusted to follow and becomes something it cannot do.
- Branch-protection changes, or any relaxation of a required check.
- Merge rights. Every change the harness produces is reviewed and merged by a human.
- Any classic scope beyond `public_repo`: no `repo`, no `workflow`, no `admin:*`, no `write:org`, no `delete_repo`.
- Write access to the product repository. The account owns the fork and nothing else; it is not, and must not become, a collaborator upstream.

## Verification

Each command below confirms one step, so this document checks itself. Run them from the harness repository root.

| Prerequisite | Command | Expected |
|---|---|---|
| Nathan's GitHub handle confirmed and written to `.harness/trust.txt` | `grep -c NATHAN_HANDLE .harness/trust.txt` | `nathan-handle` reported as done |
| Machine account `jgoetzmann-bot` created | `harness setup --tier 2` | `account` reported as done |
| `Bright-Bots-Initiative/brightboost` forked into the machine account as `jgoetzmann-bot/brightboost` | `harness setup --tier 2` | `fork` reported as done |
| Actions enabled on the fork | *(no automated check)* | confirm by hand in the GitHub interface |
| Classic PAT on `jgoetzmann-bot`, scope `public_repo` only, present in `.env` as `HARNESS_GITHUB_TOKEN` | `harness doctor` | `classic-pat` reported as done |
| `claude setup-token` run and saved as repository secret `CLAUDE_CODE_OAUTH_TOKEN` | *(no automated check)* | confirm by hand in the GitHub interface |
| The classic PAT saved as repository secret `HARNESS_GITHUB_TOKEN` | *(no automated check)* | confirm by hand in the GitHub interface |
| `jgoetzmann/bright-bots-harness` made public | `harness setup --tier 2` | `public-repo` reported as done |
| Branch protection on `main`: one approving review, no force-push | *(no automated check)* | confirm by hand in the GitHub interface |
| `.github/CODEOWNERS` committed naming you for `.harness/`, `prompts/`, `.github/`, `gates.py`, `redact.py` | `cat .github/CODEOWNERS` | `codeowners` reported as done |
| The twelve `harness:*` state labels exist on `jgoetzmann/bright-bots-harness` | `harness init --labels` | `labels` reported as done |
| Pinned tracking issue opened and its number recorded as `TRACKING_ISSUE` | `harness doctor` | `tracking-issue` reported as done |
| Decided whether Nathan reviews proposals; `CODEOWNERS` covers `/proposals/` | `grep -n proposals .github/CODEOWNERS` | `proposals-codeowners` reported as done |
| Verified whether `claude --max-budget-usd` binds under subscription auth | `grep -n max-budget-usd DECISIONS.md` | `max-budget-experiment` reported as done |
| Docker Desktop installed and `docker info` succeeds (local mode only) | `docker info` | `docker` reported as done |
| Nathan told this exists, before the first delivery PR arrives | *(no automated check)* | confirm by hand in the GitHub interface |
| `PERMISSION_TIER` raised to 2 | `harness doctor` | `tier` reported as done |
| `jgoetzmann-bot` is NOT a collaborator on `Bright-Bots-Initiative/brightboost` — confirmed | *(no automated check)* | confirm by hand in the GitHub interface |

- `harness setup --tier 2` exits 0 when every prerequisite above is satisfied, and 6 while any remains outstanding.
- `harness doctor` re-probes the environment rather than trusting anything recorded here.
- Scanning this file with the harness redaction patterns must yield no match; it names keys and scopes only.
