# HUMAN.md — Bright Bots Harness setup

This is a gap report, regenerated by `harness setup`. It describes the distance between what is configured now and what the target tier needs, and it shrinks as prerequisites are satisfied. It contains no secret value — only key names, scopes, and who must act.

## Current state

- Current tier: **0** — Tier 0 — read only, no credential
- Target tier: **1** — Tier 1 — comment on issues and file issues
- Overall: **not ready** — 0 of 5 prerequisites satisfied

| Prerequisite | Actor | Needed from tier | Status |
|---|---|---|---|
| GitHub account `brightboost-harness` exists | you | 1 | outstanding |
| Account email verified and two-factor authentication enabled | you | 1 | outstanding |
| Fine-grained PAT present in `.env` as `HARNESS_GITHUB_TOKEN` | you | 1 | outstanding |
| Fine-grained token approved for the organization | nathaniel | 1 | outstanding |
| `PERMISSION_TIER` raised to 1 | nathaniel | 1 | outstanding |

No prerequisite is satisfied yet.

## What you need to do

1. **GitHub account `brightboost-harness` exists** (actor: you)
   Create the `brightboost-harness` GitHub account. Confirm the current GitHub terms permit a machine account, that you own it, and that you are answerable for what it does.
   Confirm with: `harness setup --tier 1`

2. **Account email verified and two-factor authentication enabled** (actor: you)
   Verify the email address on `brightboost-harness` and enable two-factor authentication. Required before the account can join an organization. Nothing outside the account can check this, so confirm it by hand and treat this line as a reminder, not a failure.

3. **Fine-grained PAT present in `.env` as `HARNESS_GITHUB_TOKEN`** (actor: you)
   Generate a fine-grained personal access token with exactly the permission set in `## Tokens` below, scoped to `Bright-Bots-Initiative/brightboost` alone, with the shortest expiry you will tolerate re-issuing. Put it in `.env` under the key `HARNESS_GITHUB_TOKEN`. Never in a chat, never in a commit, never in a review package.
   Confirm with: `harness doctor`

## Tokens

| `.env` key | Type | Permission set for tier 1 | Where to create it | Who must approve it |
|---|---|---|---|---|
| `HARNESS_GITHUB_TOKEN` | Fine-grained personal access token, scoped to `Bright-Bots-Initiative/brightboost` alone — never a classic token | `Metadata`: Read; `Issues`: Read and write; `Pull requests`: Read and write; `Contents`: none; `Workflows`: none; `Administration`: none; `Secrets, Environments, Actions`: none | https://github.com/settings/personal-access-tokens/new | An organization owner (Nathaniel) when the organization restricts fine-grained tokens |

The exact permission set for tier 1, from the tier table:

| Permission | Tier 1 | Note |
|---|---|---|
| `Metadata` | Read | Mandatory for every fine-grained token |
| `Issues` | Read and write | Comments and issue creation |
| `Pull requests` | Read and write | Pull-request review comments |
| `Contents` | none | No branch is pushed at this tier |
| `Workflows` | none | Deliberately withheld — see below |
| `Administration` | none | Never |
| `Secrets, Environments, Actions` | none | Never |

Classic tokens are refused by design: classic scopes are account-wide and cannot be narrowed to one repository.

## What needs Nathaniel

Everything below needs organization or repository administration. It is gathered here so it can be sent as one request.

1. **Fine-grained token approved for the organization** (actor: Nathaniel)
   Approve the fine-grained token for the organization that owns `Bright-Bots-Initiative/brightboost`. Organizations can require an owner to approve a fine-grained token before it reaches their repositories; until that happens the token reads as valid but returns 404 on every repository path.

2. **`PERMISSION_TIER` raised to 1** (actor: Nathaniel)
   Agree Tier 1 — comment on issues and file issues, then set `PERMISSION_TIER=1` in `.env`. Nathaniel agrees the tier; you edit the file. Without this the harness refuses the token in code no matter what `.env` holds.
   Confirm with: `harness doctor`

## What the harness will never ask for

- Production credentials of any kind — database URLs, deployment keys, cloud accounts.
- Organization administration, or ownership of the organization.
- The `Workflows` permission. Without it GitHub itself rejects any push that modifies `.github/workflows/`, so "never edit CI to go green" stops being a rule the harness is trusted to follow and becomes something it cannot do.
- Branch-protection changes, or any relaxation of a required check.
- Merge rights. Every change the harness produces is reviewed and merged by a human.
- A classic personal access token. Classic scopes are account-wide and cannot be narrowed to one repository.

## Verification

Each command below confirms one step, so this document checks itself. Run them from the harness repository root.

| Prerequisite | Command | Expected |
|---|---|---|
| GitHub account `brightboost-harness` exists | `harness setup --tier 1` | `account` reported as done |
| Account email verified and two-factor authentication enabled | *(no automated check)* | confirm by hand in the GitHub interface |
| Fine-grained PAT present in `.env` as `HARNESS_GITHUB_TOKEN` | `harness doctor` | `token` reported as done |
| Fine-grained token approved for the organization | *(no automated check)* | confirm by hand in the GitHub interface |
| `PERMISSION_TIER` raised to 1 | `harness doctor` | `tier` reported as done |

- `harness setup --tier 1` exits 0 when every prerequisite above is satisfied, and 6 while any remains outstanding.
- `harness doctor` re-probes the environment rather than trusting anything recorded here.
- Scanning this file with the harness redaction patterns must yield no match; it names keys and scopes only.
